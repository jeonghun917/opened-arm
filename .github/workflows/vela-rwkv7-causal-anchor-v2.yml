name: VELA RWKV7 functional causal anchor v2

on:
  push:
    branches: [vela-experiment-infra]
    paths:
      - 'experiments/vela-active-state/rwkv7-causal-anchor-v2/**'
      - '.github/workflows/vela-rwkv7-causal-anchor-v2.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: vela-rwkv7-causal-anchor-v2
  cancel-in-progress: false

jobs:
  rwkv7-causal-anchor-v2:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: vela-experiment-infra
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install runtime
        run: |
          python -m pip install --upgrade pip
          python -m pip install 'torch>=2.2,<3' huggingface_hub numpy
      - name: Run RWKV7 functional causal-anchor replication
        env:
          HF_HUB_DISABLE_TELEMETRY: '1'
          VELA_RESULT_PATH: ops/vela-results/rwkv7-causal-anchor-v2-latest.json
        run: python experiments/vela-active-state/rwkv7-causal-anchor-v2/rwkv7_causal_anchor_v2.py
      - name: Commit result snapshot
        if: always()
        run: |
          if [[ ! -f ops/vela-results/rwkv7-causal-anchor-v2-latest.json ]]; then
            mkdir -p ops/vela-results
            printf '{"status":"FAIL_BEFORE_RESULT","source_commit":"%s"}\n' "$GITHUB_SHA" > ops/vela-results/rwkv7-causal-anchor-v2-latest.json
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add ops/vela-results/rwkv7-causal-anchor-v2-latest.json
          git diff --cached --quiet || git commit -m 'Record VELA RWKV7 causal anchor v2 [skip ci]'
          for n in 1 2 3 4; do
            git pull --rebase origin vela-experiment-infra && git push origin HEAD:vela-experiment-infra && break
            sleep $((n*3))
          done
