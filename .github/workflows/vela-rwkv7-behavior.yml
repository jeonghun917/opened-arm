name: VELA RWKV-7 actual-weight semantic behavioral probe

on:
  push:
    branches: [vela-experiment-infra]
    paths:
      - 'experiments/vela-active-state/rwkv7-behavior-v1/**'
      - '.github/workflows/vela-rwkv7-behavior.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  rwkv7-behavior:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: vela-experiment-infra
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install runtime
        run: |
          python -m pip install --upgrade pip
          python -m pip install 'torch>=2.2,<3' huggingface_hub numpy
      - name: Run pinned official RWKV-7 semantic state probe
        env:
          HF_HUB_DISABLE_TELEMETRY: '1'
          VELA_RESULT_PATH: ops/vela-results/rwkv7-behavior-latest.json
        run: python experiments/vela-active-state/rwkv7-behavior-v1/rwkv7_semantic_balanced.py
      - name: Commit result snapshot
        if: always()
        run: |
          if [[ ! -f ops/vela-results/rwkv7-behavior-latest.json ]]; then
            mkdir -p ops/vela-results
            printf '{"status":"FAIL_BEFORE_RESULT","source_commit":"%s"}\n' "$GITHUB_SHA" > ops/vela-results/rwkv7-behavior-latest.json
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add ops/vela-results/rwkv7-behavior-latest.json
          git diff --cached --quiet || git commit -m 'Record VELA RWKV-7 behavior probe [skip ci]'
          for i in 1 2 3 4; do
            git pull --rebase origin vela-experiment-infra || true
            git push origin HEAD:vela-experiment-infra && break
            sleep $((i * 3))
          done
